extends layout

block content
    div(class="container mt-3 mx-auto")
        
        div(id="usuario")

        div(class='conteiner')
                div(class='jumbotron')
                    h1(style="color:blue;") Registro de Usuario
                    form(action="/register" method="post" role="form" autocomplete="off") 
                        label
                            b Ingrese su Mail
                        input(id="nombre" name='username' placeholder="mail" class="form-control" type="text" required)
                        label
                            b Ingrese su Contraseña
                        input(id="password" name='password' placeholder="password" class="form-control" type="password" required)
                        button(class="btn btn-success mt-5 mb-5" type="submit" value="Register") Enviar
                    p(class="border-bottom border-4 w-auto") 
                    button(class="btn btn-success mt-5 mb-5" onclick="location.href='/login'") Ir a Login